<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "{{ schema.type }}",
    "additionalType": "{{ schema.subtype }}",
    "name": "{{ company.name }}",
    "description": "{{ seo[app.request.locale].description }}",
    "@id": "",
    "url": "{{ schema.url }}",
    "email":"{{ company.email }}",
    "phone":"{{ company.phone }}",
    "logo": "{{ schema.logo }}",
    "image": "{{ schema.image }}",
    "hasMap": "{{ location.link }}",
    {% include '@docono.company/docono_company/partial.address.html.twig' %}
    "geo": {
      "@type": "GeoCoordinates",
   	  "latitude": "{{ location.lat }}",
      "longitude": "{{ location.long }}"
    },
    {% include '@docono.company/docono_company/partial.times.html.twig' %}
    "sameAs": [
{% for media in socialmedia %}
  {% if media != '' %}
      "{{ media }}",
  {% endif %}
{% endfor %}
    ]
  }
</script>